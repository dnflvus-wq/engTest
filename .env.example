# Google Gemini API 키 (필수 - 문제 자동 생성 및 OCR용)
GEMINI_API_KEY=your-gemini-api-key

# Database Configuration (Docker Compose에서는 자동 설정됨)
# 로컬에서 서버 DB에 접속하려면:
# DB_HOST=서버_IP_주소
# DB_PORT=23146
DB_HOST=localhost
DB_PORT=3306
DB_NAME=engtest
DB_USER=engtest
DB_PASSWORD=engtest123!
# 주의: 서버 DB 접속 시 보안 그룹(방화벽)에서 23146 포트를 열어줘야 합니다.
