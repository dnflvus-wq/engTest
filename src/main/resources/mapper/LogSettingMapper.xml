<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.engTest.mapper.LogSettingMapper">

    <resultMap id="logSettingResult" type="com.example.engTest.dto.LogSetting">
        <id property="id" column="id"/>
        <result property="settingKey" column="setting_key"/>
        <result property="settingValue" column="setting_value"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="logSettingResult">
        SELECT * FROM log_settings ORDER BY setting_key
    </select>

    <select id="findByKey" resultMap="logSettingResult">
        SELECT * FROM log_settings WHERE setting_key = #{settingKey}
    </select>

    <update id="update">
        UPDATE log_settings SET setting_value = #{settingValue} WHERE setting_key = #{settingKey}
    </update>

    <insert id="insertIfNotExists">
        INSERT IGNORE INTO log_settings (setting_key, setting_value) VALUES (#{settingKey}, #{settingValue})
    </insert>

</mapper>
